# Aggregate of common packages across all projects.
# Recommended: install per-project requirements instead — each folder has its own requirements.txt.
#
# To install just what one project needs:
#   pip install -r <project_folder>/requirements.txt
#
# To install everything in this list (heavy, ~3 GB):
#   pip install -r requirements.txt

openai>=1.0.0
python-dotenv>=1.0.0
tiktoken>=0.5.0

# Project 02 / 10 — Gemini
google-genai>=1.0.0

# Project 03 / 08
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
ollama>=0.1.0

# Project 04 — HuggingFace (heavy)
transformers>=4.40.0
torch>=2.0.0
sentencepiece>=0.2.0
accelerate>=0.30.0

# Project 05 / 06
requests>=2.31.0
rich>=13.0.0

# Project 07 / 08 — RAG
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-qdrant>=0.2.0
langchain-text-splitters>=0.3.0
pypdf>=4.0.0
qdrant-client>=1.10.0

# Project 08 — Redis queue
pydantic>=2.0.0
redis>=5.0.0
rq>=1.16.0
rq-dashboard>=0.6.0

# Project 10 — LangGraph
langgraph>=1.0.0
langgraph-checkpoint-mongodb>=0.4.0
langchain-classic>=1.0.0
pymongo>=4.5.0
typing-extensions>=4.7.0

# Project 11 — mem0 (PINNED to v0.1.x for Neo4j graph_store support)
mem0ai==0.1.116
neo4j>=5.0.0
langchain-neo4j>=0.4.0
rank-bm25>=0.2.2
